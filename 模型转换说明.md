# 模型转换说明

> Qwen2.5-1.5B → RKNN 格式，部署到 RK3588 NPU 的前置步骤
>
> ⚠️ 提交代码中**不包含** `.rknn` 模型文件（体积大），板子到手后按本文档转换。

---

## 一、为什么要转模型

RK3588 的 NPU 只能运行 `.rknn` 格式的模型，不能直接跑 HuggingFace 的 PyTorch 权重。转换过程：

```
Qwen2.5-1.5B (HuggingFace)
        │  ① 导出
        ▼
  .onnx 模型文件
        │  ② 转换 + 量化
        ▼
 .rknn 模型文件  ← 板子要的就是这个
```

---

## 二、准备工作

| 项目 | 要求 |
|------|------|
| 转换机器 | x86 电脑（Windows/Linux 均可），**不一定要在板子上** |
| Python | 3.8 ~ 3.12 |
| 磁盘 | 至少 10GB 空闲（模型权重 ~3GB） |
| 网络 | 能访问 HuggingFace（或提前下载好模型） |

---

## 三、转换步骤

### 第 1 步：安装依赖

```bash
pip install rknn-toolkit2 transformers torch
```

> `rknn-toolkit2` 是瑞芯微官方的转换工具，只在 x86 上装，板子上不用装这个。

### 第 2 步：导出 ONNX

```bash
python3 << 'EOF'
from transformers import AutoModelForCausalLM, AutoTokenizer
from transformers.onnx import export
from pathlib import Path

model_name = "Qwen/Qwen2.5-1.5B-Instruct"

print("下载模型...")
model = AutoModelForCausalLM.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# 导出 ONNX
out_dir = Path("qwen_onnx")
out_dir.mkdir(exist_ok=True)

print("导出 ONNX...")
# 方法1: 用 optimum（推荐，更稳）
# pip install optimum[onnxruntime]
# optimum-cli export onnx --model Qwen/Qwen2.5-1.5B-Instruct qwen_onnx/

# 方法2: 用 transformers 原生导出（简单模型适用）
from transformers.onnx import FeaturesManager
model_kind, model_onnx_config = FeaturesManager.check_supported_model_or_raise(model)
onnx_config = model_onnx_config(model.config)
onnx_inputs, onnx_outputs = export(
    tokenizer, model, onnx_config, onnx_config.default_onnx_opset,
    out_dir / "model.onnx"
)
print("ONNX 导出完成:", out_dir / "model.onnx")
EOF
```

> 💡 如果 transformers 原生导出报错（LLM 的 KV-cache 结构复杂），
> 直接用 `optimum-cli`（上面注释里的一行命令）最省事。

### 第 3 步：ONNX → RKNN（含 int8 量化）

```python
from rknn.api import RKNN

rknn = RKNN()

# 配置：目标平台 + int8 量化（4GB 内存推荐 int8，能省一半内存）
rknn.config(
    mean_values=[[0, 0, 0]],
    std_values=[[255, 255, 255]],
    target_platform="rk3588",
    quantized_dtype="w8a8",          # int8 量化；内存紧张可用 w4a8
)

print("加载 ONNX...")
ret = rknn.load_onnx(model="qwen_onnx/model.onnx")
assert ret == 0, f"load_onnx 失败: {ret}"

print("构建 RKNN 模型（这一步最慢，可能要几分钟到半小时）...")
ret = rknn.build(do_quantization=True)
assert ret == 0, f"build 失败: {ret}"

print("导出...")
ret = rknn.export_rknn("qwen2.5-1.5b.rknn")
assert ret == 0, f"export 失败: {ret}"

rknn.release()
print("✅ 完成！生成 qwen2.5-1.5b.rknn")
```

### 第 4 步：拷到板子

```bash
scp qwen2.5-1.5b.rknn user@rk3588:~/揭榜挂帅/
```

放的位置就是 `sglang_1_1_module.py` 同目录，这样主文件会自动找到。

---

## 四、量化精度选择

| 量化 | 内存占用 | 精度 | 适用 |
|------|---------|------|------|
| `w8a8` (int8) | ~1.5 GB | 高，接近 fp16 | **推荐**，4GB 板子能跑 |
| `w4a8` (int4权重) | ~0.8 GB | 中，略降 | 内存极度紧张时 |
| `fp16` | ~3 GB | 最高 | 8GB+ 板子，4GB 跑不下 |

4GB 板子**建议 `w8a8`**。

---

## 五、常见问题

### Q: `load_onnx` 报错说 shape 不支持

LLM 模型结构复杂（attention 的 KV cache、动态 shape）。解决办法：
- 用 `optimum-cli` 重新导出，它会处理好这些
- 或到 HuggingFace 找现成的 ONNX 版本：`Qwen/Qwen2.5-1.5B-Instruct` 的 Model Card 里找 "ONNX" 标签

### Q: `build` 跑了几分钟就报内存不足

转换时 ONNX 模型 + 量化中间结果都在内存里，1.5B 模型需要 ~8GB 内存。x86 机器内存不够的话：
- 换 0.5B 模型（`Qwen/Qwen2.5-0.5B-Instruct`），内存减半
- 或用 `w4a8` 量化

### Q: 转换好了但板子上加载失败

检查三点：
1. `target_platform` 必须是 `"rk3588"`（不是 rk3588s、不是 rk3568）
2. 板子 NPU 驱动已加载：`lsmod | grep rknpu`
3. 版本匹配：rknn-toolkit2（转换用）和 rknn-toolkit-lite2（板子用）主版本要一致

### Q: 没有 .rknn 模型时能先跑吗？

能。代码会自动降级到 CPU 兜底模式，先验证监控和决策逻辑：

```bash
cd 揭榜挂帅
python3 test_dynamic_control.py   # 8 用例，不依赖模型
python3 demo_dynamic_control.py   # 6 场景决策演示
```

等模型转好再加载 NPU。

---

## 六、时间预估

| 步骤 | 时间 |
|------|------|
| 装依赖 | 10 min |
| 下载模型 | 5~20 min（看网速） |
| 导出 ONNX | 5 min |
| ONNX → RKNN build | 10~40 min（看机器配置） |
| 拷到板子 | 1 min |

**总计约 1~1.5 小时**，主要变量在 `build` 和网络下载。
