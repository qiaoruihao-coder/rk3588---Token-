# 1.1 模块 修改日程（变更记录）

> 记录交付包的历次实质改动，供评审 / 答辩 / 追溯用。

---

## 一、当前仓库定位

**本仓库为"最初合集"版**（GPU/outlines + Ollama + RK3588 + 动态控制四套）。

**关键架构说明（对齐老师核心任务 1.1）**：
- 老师 1.1 要求"依托底层正则解码算子，在机床(token采样)层面控制输出，强制只出 JSON/Diff/纯结论，熔断废话"。
- 为此 RK3588 正式部署采用 **llama.cpp 的 JSON-schema grammar 硬约束解码**（非事后校验），
  Ollama（原生 structured outputs）作备选。SGLang/vLLM 系 x86+GPU，RK3588 上无法运行。

---

## 二、变更记录

| # | 日期 | 主题 | 内容 |
|---|------|------|------|
| 1 | 8/17 | 重建仓库为"最初合集" | 弃用 RKLLM 版，仓库改为最初合集（GPU/outlines+Ollama+RK3588+动态控制），force push 覆盖 |
| 2 | 8/17 | 新增约束解码后端 | `llamacpp_backend.py`（JSON-schema grammar 硬约束）+ `ollama_backend.py`（structured outputs）；controller 主后端改接 llama.cpp，替换弃用的 RKNBackend(rknnlite) 空壳；`generate_json` 走 `structured=True` 约束解码路径 |
| 3 | 8/17 | 完善动态控制-模型档位动态选择 | 按老师要求"根据 NPU/温度/内存实时调节**模型的选择**、上下文长度"：config 新增 `model_tiers`(level→tier: normal/idle=full, warning=mid, critical=tiny) + `model_tier_catalog`(每档对应具体模型名/量化)；evaluator 决策时按稳定等级设 `Decision.model_tier`；controller 把 tier 传给 backend；llama.cpp 后端 `_resolve_model(tier)` 按档位选模型(实测 normal→q4_k_m, warning→q2_k, critical→tiny)；Ollama 后端补齐 tier 参数；status() 暴露 model_tier |

---

## 三、核心任务 1.1 符合度

| 要求 | 实现 | 状态 |
|------|------|------|
| 底层正则解码 / 机床层硬约束 | llama.cpp JSON-schema grammar（token 层锁死） | ✅ 真约束解码 |
| 只输出 JSON / Diff / 纯结论 | generate_json/label/value/diff 分派 + 约束解码 | ✅ |
| 熔断推理过程与废话 | 采样层 grammar 硬锁，不回退为自由生成 | ✅ |
| RK3588 部署 | llama.cpp（主）+ Ollama（备） | ✅ |
| **动态控制**：按 NPU/温度/内存调节 | Monitor 采集 → 状态机 → warning 缩 context/tokens、critical 最小；**模型档位随状态切换(full/mid/tiny)** | ✅ |

> 注：本机（Windows，无 llama-server/模型）仅完成后端逻辑与 mock 驱动验证；
> 真机 RK3588 上的 llama.cpp grammar 解码效果需上板实测（见 SETUP_RK3588.md）。

---

## 四、待办 / 注意

- RK3588 上装 llama.cpp（arm 可编）或 ollama(arm)，拉取量化 .gguf（如 qwen2.5:1.5b）
- 为"模型档位切换"准备多档 .gguf（full: 1.5b q4_k_m / mid: 1.5b q2_k / tiny: 0.5b）放入 llama-server 可加载集合
- 实测约束解码质量、TTFT、内存（≤1.5GB 硬指标）
- 动态控制（模型档位/context/温度频率）与 llama.cpp 服务的真机联调